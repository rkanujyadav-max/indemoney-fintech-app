export function getRouter() {
  // Minimal placeholder router used only to satisfy build.
  // Replace this with a real TanStack Router instance when ready.
  return {};
}

const router = getRouter();
export default router;
